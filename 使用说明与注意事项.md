# 三花日常 · 桌面版 使用说明与注意事项

> 本文档说明：怎么把它变成「不依赖浏览器、离线也能用、数据不丢」的桌面 App，
> 以及几个你必须知道的**现实限制**和**替代方案**。请先读「注意事项」一节。

---

## 一、你的需求 vs 现实能力（先看清）

| 你的要求 | 能否做到 | 说明 |
|---|---|---|
| 桌面 App、不依附浏览器标签页 | ✅ 能 | 用 Electron 打包，独立窗口运行 |
| 离线 / 在线都稳定使用 | ✅ 能 | 数据存在本地，联网与否都不影响打开 |
| 数据不会丢失 | ✅ 能（有前提） | 见「数据不丢的三道防线」 |
| 跨设备自动同步 | ⚠️ 部分能 | 没有服务器，靠「云盘同步文件夹」或手动导入导出 |
| 我（AI）直接给你一个 .exe 安装包 | ❌ 不能 | 见「现实困境①」 |

---

## 二、现实困境（为什么不能一步到位）

**困境①：我这个运行环境无法编译 Windows 安装包**
- 打包 .exe 需要 Windows 的编译工具链（Electron Builder / NSIS），本环境没有。
- 所以**最后一步「在你电脑上跑一次 npm」必须由你自己完成**（只需一次，免费的）。
- 这不是偷懒，是客观限制。我负责把代码、配置、图标全部写好，你复制一条命令即可。

**困境②：没有服务器 = 没有真正的「云端账号同步」**
- App 从一开始就是纯本地、无账号、无后端。
- 跨设备同步只能靠「把数据文件放进云盘同步文件夹」，或手动导出/导入。
- 如果你想要「多设备实时无缝同步」，需要加一个后端（如 Supabase），那是另一项工程，可以以后再做。

**困境③：线上那个网址（CloudStudio 免费托管）可能被「闲置回收」**
- 免费沙盒长时间没人访问会被休眠或删除 → 网址打不开。
- 但**你的数据存在自己设备/本地文件里，不会因此丢失**。
- 桌面版 App 完全不依赖这个网址，所以这是最稳的形态。

---

## 三、三种运行方式（任选）

### 方式 A：立刻能用（零安装）— 启动器 .bat ⭐
1. 双击 `启动桌面版.bat`
2. 它会用你电脑上的 Chrome / Edge 以「应用窗口」打开 `standalone.html`
3. 得到：独立窗口、无浏览器标签栏、离线可用
- **前提**：电脑装了 Chrome 或 Edge（基本都有）
- **数据位置**：仍在该浏览器的本地存储里（比纯网页耐用，但清浏览器数据会没）

### 方式 B：真·桌面 App（推荐，最稳）— Electron
> 数据存成本地 JSON 文件，彻底独立于浏览器，最耐用。

第一次（只需一次，在你自己电脑上）：
```
cd 三花日常文件夹
npm install
npm start          # 直接运行桌面 App，不用打包
```
想生成可安装的 .exe 安装包（发给别人/固定安装）：
```
npm run dist        # 需要联网下载 electron + NSIS，产出 dist/三花日常-setup.exe
```
- 运行后即可在桌面/开始菜单看到「三花日常」，点开即全屏窗口，离线照用。

### 方式 C：手机装 App（PWA）
- 手机 Chrome 打开网址 → 菜单「安装应用」/「添加到主屏幕」
- 或用 pwabuilder.com 生成 .apk
- 注意：仍依赖那个网址在线（见困境③）

---

## 四、数据不丢的三道防线

1. **自动存本地**：任何改动约 0.3 秒后自动写入本地（浏览器存储 / Electron 本地文件）。
2. **导出备份**：设置页 → 「导出备份」下载一个 JSON 文件，存到网盘/微信文件传输助手。
3. **云盘同步（仅桌面版）**：设置页「云盘同步」→「移动到云盘同步文件夹」，
   选 OneDrive / 百度网盘同步版 / iCloud 的同步目录，
   之后数据文件会被这些云盘自动跨设备同步。

> 建议：选**方式 B（Electron）** 当主力 + 偶尔点一次「导出备份」存网盘，万无一失。

---

## 五、重要注意事项（必读）

1. **不同打开方式 = 不同的数据孤岛**：手机填的、电脑双击 standalone 填的、Electron 填的，
   互相看不到。固定用一个主力入口，别多处同时填。
2. **standalone.html 别乱挪位置**：它的数据跟「文件所在路径」绑定，挪到别的文件夹旧数据就找不到了。
   固定放一个地方（如桌面或某文件夹）。
3. **离线时只有两个附加功能会变空**：天气（open-meteo）、影视查剧（TMDB）需要联网；
   你的足迹、图片、日记、记账等核心功能**全部离线可用**。
4. **Electron 首次需要 npm install**：约几十 MB 下载，之后启动极快。
5. **迁移/换电脑**：用「导出备份」拿 JSON → 新电脑「导入备份」即可合并，无需重来。

---

## 六、文件清单（本文件夹）

| 文件 | 作用 |
|---|---|
| `standalone.html` | 单文件离线版，双击即用（零外链） |
| `启动桌面版.bat` | Windows 一键开独立窗口（方式 A） |
| `main.js` / `preload.js` | Electron 主进程 / 桥接（方式 B） |
| `package.json` | Electron 工程配置与启动脚本 |
| `index.html` / `css/` / `js/` | 网页版源码（在线/打包共用） |
| `icon-*.png` / `icon.svg` | 三花猫图标（桌面/手机安装用） |
| `使用说明与注意事项.md` | 本文件 |
